{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/feat/sgf-oss/public",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "api/**", "services/**", "subscribers/**"]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    }
  }
}
